
server:
  port: 8083

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Gisiona/microservices-spring-boot-cloud-repo-config-server
          search-paths: fornecedor