
server:
  port: 8083

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Gisiona/spring-boot-cloud-microservices-repository-config-server
          search-paths: fornecedor